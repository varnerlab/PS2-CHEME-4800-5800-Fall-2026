name: Release PS2 student assignment

on:
  push:
    tags:
      - 'ps2-cheme-4800-5800-2026.*'

permissions:
  contents: write

jobs:
  validate-and-draft:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.12.7'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Check release tag
        shell: bash
        run: |
          if [[ ! "${GITHUB_REF_NAME}" =~ ^ps2-cheme-4800-5800-2026\.[0-9]+$ ]]; then
            echo "Invalid PS2 release tag: ${GITHUB_REF_NAME}" >&2
            exit 1
          fi

      - name: Build and test the student source archive
        run: python3 instructor/validate_release.py --tag "${GITHUB_REF_NAME}"

      - name: Create draft GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --draft \
            --verify-tag \
            --title "PS2: Escape from Olin Hall with a missing CornellID" \
            --notes-file instructor/release-notes.md
